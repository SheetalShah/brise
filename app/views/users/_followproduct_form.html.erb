<% if @current_user.following_product?(product) %>
  <%= render 'users/unfollowproduct', :product => product, :type => type %>
<% else %>
  <%= render 'users/followproduct', :product => product, :type => type %>
<% end %>

